var EventsEmitter = require('events').EventEmitter();

// Schedule recurring event
exports.schedule = function() {
};

// Schedule recurring event that will only callback one process's listeners
exports.scheduleSingle = function() {
};